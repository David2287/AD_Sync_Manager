# Base URL for the Go backend API.
# In development the Vite dev server proxies /api → this address.
# In production the React build is served by Go itself, so this variable
# is not needed at runtime (the browser is already on the same origin).
VITE_API_BASE_URL=http://localhost:8080
